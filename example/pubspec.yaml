name: slidable_button_example
description: Demonstrates how to use the slidable_button plugin.

publish_to: 'none' 

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  font_awesome_flutter: ^9.1.0
  increment_button:
    path: ../


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
